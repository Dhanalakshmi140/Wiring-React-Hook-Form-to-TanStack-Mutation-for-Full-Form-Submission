{
  "name": "wiring-react-hook-form-to-a-create-mutation",
  "version": "1.0.0",
  "private": true,
  "description": "Wire a React Hook Form to a TanStack create mutation.",
  "scripts": {
    "setup": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
